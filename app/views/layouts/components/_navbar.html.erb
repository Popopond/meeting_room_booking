<nav class="bg-blue-600 shadow-lg">
  <div class="container mx-auto flex justify-between items-center p-4">
    <a href="<%= root_path %>" class="flex items-center space-x-2 text-3xl font-extrabold text-white hover:text-gray-200 transition duration-300 ease-in-out">
      <%= render 'icon/logo' ,class: " w-52 h-52 " %> 
      <span>Sync</span>
    </a>

    <div class="flex space-x-8 items-center">
      <div class="flex space-x-6">
        <%= link_to "Home", root_path, class: "text-lg text-white hover:text-yellow-300 transition duration-300 ease-in-out" %>
        <%= link_to "Room", rooms_path, class: "text-lg text-white hover:text-yellow-300 transition duration-300 ease-in-out" %>
        <%= link_to "จองห้อง", bookings_path, class: "text-lg text-white hover:text-yellow-300 transition duration-300 ease-in-out" %>
        <%= link_to "สถานะห้อง", statuses_path, class: "text-lg text-white hover:text-yellow-300 transition duration-300 ease-in-out" %>
        <%= link_to "เช็คอิน", new_check_in_path, class: "text-lg text-white hover:text-yellow-300 transition duration-300 ease-in-out" %>
      </div>

      <div class="flex space-x-6 items-center">
        <% if user_signed_in? %>
          <span class="text-sm text-white">Welcome, <%= current_user.email %></span>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "border-2 border-white bg-red-600 text-white px-6 py-2 rounded-lg shadow-md hover:bg-red-700 hover:border-yellow-300 transition duration-300 ease-in-out" %>
        <% else %>
          <%= link_to "Log in", new_user_session_path, class: "border-2 border-white bg-yellow-500 text-white px-6 py-2 rounded-lg shadow-md hover:bg-yellow-600 hover:border-blue-300 transition duration-300 ease-in-out" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
