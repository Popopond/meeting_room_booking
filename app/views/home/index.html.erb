<div class="container mx-auto px-4 py-8">
  <div class="text-center mb-12">
    <img src="<%= asset_path('oddlogo.svg') %>" alt="OddSync Logo" class="w-32 h-32 mx-auto mb-6">
    <h1 class="font-bold text-4xl mb-4">ยินดีต้อนรับสู่ ODDSYNC</h1>
    <p class="text-xl text-gray-600">ระบบจองห้องประชุมออนไลน์</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
    <div class="bg-white rounded-lg shadow-md p-6">
      <h2 class="text-2xl font-semibold mb-4">การจองที่กำลังจะถึง</h2>
      <% if @upcoming_bookings.any? %>
        <ul class="space-y-4">
          <% @upcoming_bookings.each do |booking| %>
            <li class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
              <div>
                <h3 class="font-medium"><%= booking.room.name %></h3>
                <p class="text-sm text-gray-600">
                  <%= booking.start_time.strftime("%d/%m/%Y %H:%M") %>
                </p>
              </div>
              <%= link_to "ดูรายละเอียด", booking_path(booking), 
                  class: "text-blue-500 hover:text-blue-700" %>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="text-gray-500">ไม่มีการจองที่กำลังจะถึง</p>
      <% end %>
    </div>

    <div class="bg-white rounded-lg shadow-md p-6">
      <h2 class="text-2xl font-semibold mb-4">การจองล่าสุด</h2>
      <% if @recent_bookings.any? %>
        <ul class="space-y-4">
          <% @recent_bookings.each do |booking| %>
            <li class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
              <div>
                <h3 class="font-medium"><%= booking.room.name %></h3>
                <p class="text-sm text-gray-600">
                  <%= booking.start_time.strftime("%d/%m/%Y %H:%M") %>
                </p>
              </div>
              <%= link_to "ดูรายละเอียด", booking_path(booking), 
                  class: "text-blue-500 hover:text-blue-700" %>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="text-gray-500">ยังไม่มีประวัติการจอง</p>
      <% end %>
    </div>
  </div>

  <div class="mt-12 text-center">
    <%= link_to "จองห้องประชุม", new_booking_path, 
        class: "bg-blue-500 text-white px-8 py-3 rounded-lg text-lg hover:bg-blue-600 transition duration-200" %>
  </div>
</div>
