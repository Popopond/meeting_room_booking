<div class="flex items-center justify-between px-8 py-4">
  <!-- Left: Title and Date -->
  <div class="flex flex-col">
          <%= render "shared/welcome" %>
<!--    <h1 class="text-2xl font-bold text-black" data-testid="header-title">Meeting Room Booking</h1>
-->    <div class="text-lg text-gray-700 mt-1 text-center" data-date-selector-target="monthDisplay" data-testid="month-display">
      <%= @start_date&.strftime("%-d %B %Y").gsub(/April/, 'เมษายน') || Date.current.strftime("%-d %B %Y").gsub(/April/, 'เมษายน') %>
    </div>
  </div>

  <!-- Center: Booking Steps -->
  <div class="flex-1 flex justify-center">
    <%= render "shared/booking_steps" %>
  </div>

  <!-- Right: Actions -->
  <div class="flex items-center space-x-4">
    <%= link_to "#", 
        class: "h-10 px-6 rounded-xl bg-violet-600 text-white flex items-center gap-2 opacity-50 cursor-not-allowed add-event-button hover:bg-violet-700 transition-colors shadow-sm", 
        data: { turbo: false },
        data_testid: "add-event-button" do %>
      <%= render "shared/icons/add_event" %>
      จองห้องประชุม
    <% end %>
  </div>
</div> 