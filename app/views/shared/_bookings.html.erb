<div class="grid grid-cols-1 md:grid-cols-2 gap-8" data-testid="bookings-section">
  <div class="bg-white rounded-lg shadow-md p-6" data-testid="upcoming-bookings">
    <h2 class="text-2xl font-semibold mb-4" data-testid="upcoming-bookings-title">การจองที่กำลังจะถึง</h2>
    <% if @upcoming_bookings.any? %>
      <ul class="space-y-4" data-testid="upcoming-bookings-list">
        <% @upcoming_bookings.each do |booking| %>
          <li class="flex items-center justify-between p-4 bg-gray-50 rounded-lg" data-testid="upcoming-booking-item">
            <div>
              <h3 class="font-medium" data-testid="booking-room-name"><%= booking.room.name %></h3>
              <p class="text-sm text-gray-600" data-testid="booking-time">
                <%= booking.start_time.strftime("%d/%m/%Y %H:%M") %>
              </p>
            </div>
            <%= link_to "ดูรายละเอียด", booking_path(booking), 
                class: "text-violet-500 hover:text-violet-700",
                data_testid: "booking-details-link" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-gray-500" data-testid="no-upcoming-bookings">ไม่มีการจองที่กำลังจะถึง</p>
    <% end %>
  </div>

  <div class="bg-white rounded-lg shadow-md p-6" data-testid="recent-bookings">
    <h2 class="text-2xl font-semibold mb-4" data-testid="recent-bookings-title">การจองล่าสุด</h2>
    <% if @recent_bookings.any? %>
      <ul class="space-y-4" data-testid="recent-bookings-list">
        <% @recent_bookings.each do |booking| %>
          <li class="flex items-center justify-between p-4 bg-gray-50 rounded-lg" data-testid="recent-booking-item">
            <div>
              <h3 class="font-medium" data-testid="booking-room-name"><%= booking.room.name %></h3>
              <p class="text-sm text-gray-600" data-testid="booking-time">
                <%= booking.start_time.strftime("%d/%m/%Y %H:%M") %>
              </p>
            </div>
            <%= link_to "ดูรายละเอียด", booking_path(booking), 
                class: "text-violet-500 hover:text-violet-700",
                data_testid: "booking-details-link" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-gray-500" data-testid="no-recent-bookings">ยังไม่มีประวัติการจอง</p>
    <% end %>
  </div>
</div> 